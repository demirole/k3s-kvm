- hosts: all
  tasks:
    - debug:
        msg:
          - "Created auto inventory"
          - "Run 'ansible-playbook -i .vagrant/provisioners/ansible/inventory/vagrant_ansible_inventory create-k3s-cluster.yml' to provision"
